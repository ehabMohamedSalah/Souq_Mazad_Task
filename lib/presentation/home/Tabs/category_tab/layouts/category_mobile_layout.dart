import 'package:flutter/material.dart';

class CategoryMobileLayout extends StatelessWidget {
  const CategoryMobileLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red,);
  }
}
